// Deprecated: use scripts/capture-bindings.mjs instead.
// Kept as a placeholder so old docs/links don't break.

console.error("Use: node scripts/capture-bindings.mjs (requires playwright)");
process.exit(1);
